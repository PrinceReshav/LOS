package com.los.loanoriginatingsystem.access.dto;

/** Mirrors administration-service's visibility.model.RecordAccessLevel - kept in sync manually since these two services don't share a DTO module. */
public enum RecordAccessLevel {
    READ,
    READ_WRITE
}
